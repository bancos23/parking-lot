{
  "name": "licenta",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && py -m uvicorn main:app --reload --reload-exclude output --reload-exclude debug_outputs --reload-exclude .paddlex --reload-exclude __pycache__ --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && pip install -e .",
    "install:all": "npm run install:frontend && npm run install:backend"
  },
  "devDependencies": {
    "concurrently": "10.0.3"
  },
  "engines": {
    "node": "^22.0.0",
    "npm": "^11.13.0"
  }
}